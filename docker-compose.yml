services:
  dev:
    image: node:22-alpine
    working_dir: /app
    ports:
      - "5173:5173"
      - "3001:3001"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      VITE_API_BASE_URL: http://localhost:3001
      HUSKY: "0"
    command: sh /app/docker/dev-entrypoint.sh
